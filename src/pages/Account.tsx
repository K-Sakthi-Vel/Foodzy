import React from 'react';

export default function AccountPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold mb-4">Account</h1>
      <p>This page will host sign-in (email OTP), profile and settings.</p>
    </div>
  );
}
